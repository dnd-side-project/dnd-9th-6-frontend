import { createQueryKeys } from '../utils/createQueryKeys';

export const SCOPE_KEY = createQueryKeys('scope');

export const LECTURES_KEY = createQueryKeys('lectures');

export const BOOKMARK_KEY = createQueryKeys('bookmark');

export const USER_KEY = createQueryKeys('user');
